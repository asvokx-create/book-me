"use client";

import Link from "next/link";
import { useCallback, useEffect, useState } from "react";

type Issue = { id: string; status: string; created_at: string; reporter_name: string; reporter_email: string; admin_note: string; title?: string; details: string; steps_to_reproduce?: string; page_url?: string; category?: string; requested_resolution?: string; service_title?: string; booking_id?: string; against_name?: string; against_email?: string };

export default function AdminIssueQueue({ type }: { type: "bugs" | "disputes" }) {
  const [issues, setIssues] = useState<Issue[]>([]); const [loading, setLoading] = useState(true); const [busy, setBusy] = useState(""); const [error, setError] = useState("");
  const load = useCallback(async () => { setLoading(true); const response = await fetch(`/api/admin/issues?type=${type}`, { cache: "no-store" }).catch(() => null); const data = response ? await response.json() as { issues?: Issue[]; error?: string } : null; if (!response?.ok) setError(data?.error ?? "The queue could not be loaded."); else { setIssues(data?.issues ?? []); setError(""); } setLoading(false); }, [type]);
  useEffect(() => { const timer = window.setTimeout(() => { void load(); }, 0); return () => window.clearTimeout(timer); }, [load]);
  async function update(issue: Issue, status: "reviewing" | "resolved" | "dismissed") {
    const note = window.prompt(status === "reviewing" ? "Optional internal note:" : "Add an outcome or admin note:", issue.admin_note)?.trim(); if (note === undefined) return;
    setBusy(issue.id); const response = await fetch("/api/admin/issues", { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ type, id: issue.id, status, note }) }).catch(() => null); setBusy("");
    if (!response?.ok) { const data = response ? await response.json() as { error?: string } : null; setError(data?.error ?? "That case could not be updated."); return; } await load();
  }
  const title = type === "bugs" ? "Reported bugs" : "Booking disputes";
  return <main className="min-h-screen bg-[#f4f4ef] text-[#183126]"><header className="border-b border-[#183126]/10 bg-white"><div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-3 px-5 py-4"><Link href="/admin" className="flex items-center gap-2 font-bold"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#183126] text-[#eee25a]">B</span>BubsBookings Admin</Link><div className="flex gap-2"><Link href="/admin/reported-bugs" className="rounded-full px-4 py-2 text-sm font-bold hover:bg-[#eee25a]">Bug reports</Link><Link href="/admin/disputes" className="rounded-full px-4 py-2 text-sm font-bold hover:bg-[#eee25a]">Disputes</Link></div></div></header><div className="mx-auto max-w-6xl px-5 py-10"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#718078]">Admin case queue</p><h1 className="mt-2 text-4xl font-bold">{title}</h1></div><button onClick={() => void load()} className="rounded-full bg-white px-4 py-2 text-sm font-bold hover:bg-[#eee25a]">↻ Refresh</button></div>{error && <p className="mt-5 rounded-2xl bg-[#fff0e8] p-4 text-sm font-bold text-[#964f2c]">{error}</p>}<div className="mt-7 space-y-4">{issues.map((issue) => <article key={issue.id} className="rounded-[1.7rem] border border-[#183126]/10 bg-white p-6"><div className="flex flex-col justify-between gap-4 sm:flex-row"><div><div className="flex flex-wrap gap-2"><span className="rounded-full bg-[#fff1bf] px-3 py-1 text-xs font-bold capitalize">{issue.status}</span>{issue.category && <span className="rounded-full bg-[#edf2e9] px-3 py-1 text-xs font-bold capitalize">{issue.category.replaceAll("_", " ")}</span>}</div><h2 className="mt-3 text-xl font-bold">{issue.title ?? issue.service_title}</h2><p className="mt-1 text-sm text-[#718078]">Submitted by {issue.reporter_name} ({issue.reporter_email}) · {new Date(issue.created_at).toLocaleString()}</p></div><div className="flex flex-wrap gap-2">{issue.status === "open" && <button disabled={busy === issue.id} onClick={() => void update(issue, "reviewing")} className="rounded-full border px-4 py-2 text-sm font-bold hover:bg-[#eee25a]">Start review</button>}<button disabled={busy === issue.id} onClick={() => void update(issue, "resolved")} className="rounded-full bg-[#183126] px-4 py-2 text-sm font-bold text-white">Resolve</button><button disabled={busy === issue.id} onClick={() => void update(issue, "dismissed")} className="rounded-full px-4 py-2 text-sm font-bold hover:bg-[#fff0e8]">Dismiss</button></div></div><div className="mt-5 grid gap-4 rounded-2xl bg-[#f7f7f2] p-5"><div><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Details</p><p className="mt-2 whitespace-pre-wrap text-sm leading-6">{issue.details}</p></div>{issue.requested_resolution && <div><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Requested resolution</p><p className="mt-2 text-sm">{issue.requested_resolution}</p></div>}{issue.against_name && <div><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Against</p><p className="mt-2 text-sm">{issue.against_name} · {issue.against_email}</p></div>}{issue.steps_to_reproduce && <div><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Steps to recreate</p><p className="mt-2 whitespace-pre-wrap text-sm">{issue.steps_to_reproduce}</p></div>}{issue.page_url && <div><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Page</p><p className="mt-2 break-all text-sm">{issue.page_url}</p></div>}{issue.admin_note && <div className="rounded-xl bg-white p-4"><p className="text-xs font-bold uppercase tracking-wider text-[#718078]">Admin note</p><p className="mt-2 text-sm">{issue.admin_note}</p></div>}</div></article>)}{!loading && issues.length === 0 && <p className="rounded-[1.7rem] bg-white p-10 text-center text-[#718078]">No {type === "bugs" ? "bug reports" : "disputes"} yet.</p>}{loading && issues.length === 0 && <p className="rounded-[1.7rem] bg-white p-10 text-center text-[#718078]">Loading queue…</p>}</div></div></main>;
}
